const key="jsmOffers";let data=JSON.parse(localStorage.getItem(key)||"null")||[];const list=document.getElementById("adminList");function render(){list.innerHTML=data.map((o,i)=>`<div class="admin-row"><div><b>${o.tag}</b> — ${o.city}<br><small>${o.hotel} | ${o.details} | ${o.price}</small></div><button onclick="removeOffer(${i})">حذف</button></div>`).join("")||"<p>لم تتم إضافة عروض محلية.</p>"}function save(){localStorage.setItem(key,JSON.stringify(data));render()}function removeOffer(i){data.splice(i,1);save()}document.getElementById("offerForm").addEventListener("submit",e=>{e.preventDefault();data.push({tag:tag.value,city:city.value,hotel:hotel.value,details:details.value,price:price.value});save();e.target.reset()});render();